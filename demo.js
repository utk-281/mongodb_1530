let emp = {
  name: "abc",
  age: 45,
};

console.log(Object.keys(emp));

/* 
1) What is the default primary key field in MongoDB, and why is it unique?

2) Which command is used in mongosh to list all databases on the server?

3) What is the relationship between a database, collection, and document in MongoDB?

4) Write the mongosh command to insert multiple documents into a collection named students.

5) How is the _id field generated if not explicitly provided during document insertion?

6) What is the purpose of the MongoDB shell (mongosh)? How is it different from MongoDB Compass?

7) Write the mongosh command to insert a single document into a collection named products.

8) Which operator is used to fetch documents where age is greater than 25?

9) How do you delete a single document matching a condition using deleteOne()? Show syntax.

10) What is a projection in MongoDB? How do you fetch only the name and email fields from a user's collection?

*/
